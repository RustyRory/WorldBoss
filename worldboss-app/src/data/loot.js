'use strict';

const LOOT_CONFIG = {
  // Number of item choices offered to the player after a dungeon
  OPTIONS_COUNT: 2,

  // Item used as fallback when an enemy has no loot pool
  FALLBACK_ITEM: 'potion_heal',
};

module.exports = { LOOT_CONFIG };
